{
  "name": "sovereign-factory",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/core",
    "apps/backend"
  ],
  "scripts": {
    "build:backend": "npm --workspace @factory/backend run build",
    "start:backend": "npm --workspace @factory/backend run start"
  },
  "dependencies": {
    "express-async-errors": "^3.1.1"
  }
}
